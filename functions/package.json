{
  "name": "sharefit-functions",
  "private": true,
  "main": "lib/src/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test lib/test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^22.18.0"
  }
}
